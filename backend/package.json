{
  "name": "skillsbridge-backend",
  "version": "0.1.0",
  "private": true,
  "description": "SkillBridge backend tooling: Firestore schema/config, seeding scripts, VAPID key generator.",
  "scripts": {
    "seed": "tsx scripts/seed.ts",
    "seed:mentors": "tsx --tsconfig scripts/tsconfig.seed.json scripts/seed/seedMentorData.ts",
    "vapid:generate": "node scripts/gen-vapid.cjs"
  },
  "devDependencies": {
    "@types/node": "^20",
    "firebase": "^12.11.0",
    "firebase-admin": "^13.8.0",
    "tsx": "^4.19.3",
    "typescript": "^5"
  }
}